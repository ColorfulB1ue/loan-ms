spring:
  application:
    name: loan-ms
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB
  web:
    resources:
      static-locations:
        - classpath:/META-INF/resources/
        - classpath:/resources/
        - classpath:/static/
        - classpath:/public/
        - classpath:/uploads/

mybatis:
  mapper-locations: classpath:com/young/mapper/*.xml
  configuration:
    # 驼峰命名
    map-underscore-to-camel-case: true
    # 开启二级缓存
    cache-enabled: true

# 服务器配置
server:
  port: ${SERVER_PORT:8080}
  tomcat:
    max-threads: 200
    min-spare-threads: 20

# JWT 认证配置（默认值，可被profile配置覆盖）
jwt:
  secret: ${JWT_SECRET:dGhpcy1pcy1hLXZlcnktc3Ryb25nLWFuZC1yYW5kb20tc2VjcmV0LWtleS1mb3Itand0LWhzMjU2LWFsZ29yaXRobS1pbi1sb2FuLW1zLXRlc3QtZW52aXJvbm1lbnQ=}
  expiration: 7200000

# Actuator 监控配置
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when_authorized
